# Rithmic connectivity (optional — app runs in free-data mode without it)
async_rithmic>=0.2.0

# Data processing
pandas>=2.0.0
numpy>=1.24.0

# Technical analysis (pandas-ta preferred; native fallbacks used if absent)
# pandas-ta is optional — app works without it via native pandas/numpy fallbacks
# pandas-ta>=0.3.14b  # uncomment if your Python env supports it

# Free market data
yfinance>=0.2.40
requests>=2.31.0
beautifulsoup4>=4.12.0   # economic calendar scraping
fredapi>=0.5.0           # optional FRED macro data (requires free API key)

# UI
streamlit>=1.35.0
plotly>=5.20.0

# AI
anthropic>=0.25.0
google-genai>=1.0.0          # optional — kostenlose Alternative zu Claude

# Config & utilities
python-dotenv>=1.0.0
configparser>=5.3.0

# Async support
aiohttp>=3.9.0
aiofiles>=23.2.0
asyncio-throttle>=1.0.2
